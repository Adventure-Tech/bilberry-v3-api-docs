allOf:
  - $ref: './VatModelShort.yaml'
  - type: object
    required: [rules, validFrom]
    properties:
      rules:
        $ref: './VatRule.yaml'
      validFrom:
        $ref: '../types/ValidFrom.yaml'