type: object
required: [id, name, description, code, percentMinor, createdAt]
properties:
  id:
    $ref: '../types/ID.yaml'
  name:
    type: string
  description:
    type: string
  code:
    type: string
  percentMinor:
    $ref: '../types/MinorPercentage.yaml'
  createdAt:
    $ref: '../types/CreatedAt.yaml'