type: object
required: [ minAge, minPriceMinor, duration, meetupTime ]
properties:
  id:
    $ref: '../types/ID.yaml'
  createdAt:
    $ref: '../types/CreatedAt.yaml'
  minAge:
    type: integer
  minPriceMinor:
    type: integer
  duration:
    type: integer
  meetupTime:
    type: string
    format: time