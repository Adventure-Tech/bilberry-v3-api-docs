type: object
required: [id, provider, currency, amountMinor, createdAt, status, statusAt, bilberryPaymentId]
readOnly: true
properties:
  id:
    # TODO: extract UUID type or switch to existing ID or ULID types
    type: string
    format: uuid
  provider:
    type: string
    enum: [nets, stripe]
  currency:
    $ref: '../types/Currency.yaml'
  amountMinor:
    $ref: '../types/MinorAmount.yaml'
  createdAt:
    $ref: '../types/CreatedAt.yaml'
  status:
    type: string
    enum: [created, timed-out, completed, cancelled]
  statusAt:
    type: string
    format: date-time
  bilberryPaymentId:
    allOf:
      - $ref: '../types/ULID.yaml'
      - nullable: true