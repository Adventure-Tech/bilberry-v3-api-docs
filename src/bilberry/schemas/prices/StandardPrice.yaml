type: object
required: [type, priceMinor]
properties:
  type:
    $ref: ./PriceType.yaml
  rules:
    type: object
    required: [priceMinor]
    properties:
      priceMinor:
        $ref: ../types/MinorAmount.yaml