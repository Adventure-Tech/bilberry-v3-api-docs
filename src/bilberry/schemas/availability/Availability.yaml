type: object
properties:
  bookableId:
    $ref: '../types/Id.yaml'
  ticketOptionId:
    $ref: '../types/Id.yaml'
  start:
    type: string
    format: date-time
  end:
    type: string
    format: date-time
  availability:
    type: integer
    minimum: 0
