type: object
required: [id, createdAt, name, minAge, maxAge, resourceRequirements]
properties:
  id:
    $ref: ../types/Id.yaml
  createdAt:
    $ref: ../types/CreatedAt.yaml
  name:
    type: string
  minAge:
    $ref: ../types/Age.yaml
  maxAge:
    $ref: ../types/Age.yaml
  resourceRequirements:
    type: array
    items:
      $ref: ./ResourceRequirement.yaml
  vatModelId:
    $ref: ../types/IdWritable.yaml
