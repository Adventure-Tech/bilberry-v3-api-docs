type: object
required: [id, amount, createdAt, type]
properties:
  id:
    $ref: '../types/ULID.yaml'
  amount:
    $ref: '../types/Amount.yaml'
  createdAt:
    $ref: '../types/CreatedAt.yaml'
  type:
    type: string
    enum: [ONLINE, CHECKOUT, GIFTCAR, ...]