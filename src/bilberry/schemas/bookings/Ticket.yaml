type: object
required: [ id, bookableId, quantity ]
properties:
  id:
    $ref: '../types/ULID.yaml'
  bookableId:
    type: integer
  quantity:
    type: integer
    minimum: 1
    default: 1
    # TODO: more info about product, ticket option etc?
